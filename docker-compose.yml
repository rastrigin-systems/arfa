version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: pivot-postgres
    environment:
      POSTGRES_DB: pivot
      POSTGRES_USER: pivot
      POSTGRES_PASSWORD: pivot_dev_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./schema.sql:/docker-entrypoint-initdb.d/01-schema.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pivot"]
      interval: 5s
      timeout: 5s
      retries: 5

  adminer:
    image: adminer:latest
    container_name: pivot-adminer
    ports:
      - "8080:8080"
    environment:
      ADMINER_DEFAULT_SERVER: postgres
    depends_on:
      - postgres

  # API Server (uncomment when ready)
  # api:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: pivot-api
  #   ports:
  #     - "3001:3001"
  #   environment:
  #     DATABASE_URL: postgres://pivot:pivot_dev_password@postgres:5432/pivot?sslmode=disable
  #     JWT_SECRET: dev_secret_change_in_production
  #     PORT: 3001
  #   depends_on:
  #     postgres:
  #       condition: service_healthy

volumes:
  postgres_data:
