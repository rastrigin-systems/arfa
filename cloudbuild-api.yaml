steps:
  # Build API image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/ubik-images/api:latest'
      - '-t'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/ubik-images/api:$SHORT_SHA'
      - '-f'
      - 'services/api/Dockerfile.gcp'
      - '.'
    timeout: 600s

  # Push API image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/ubik-images/api:latest'
    timeout: 600s

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/ubik-images/api:$SHORT_SHA'
    timeout: 600s

images:
  - 'us-central1-docker.pkg.dev/$PROJECT_ID/ubik-images/api:latest'
  - 'us-central1-docker.pkg.dev/$PROJECT_ID/ubik-images/api:$SHORT_SHA'

timeout: 1200s
