# API Configuration

# Server-side API URL (for Next.js server actions)
# Use Docker network hostname when running in containers
API_URL=http://localhost:8080/api/v1

# Client-side API URL (NEXT_PUBLIC_* vars are available in browser)
# Always use localhost for browser access
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1

# Copy this file to .env.local for local development
# Docker Compose sets these automatically:
#   API_URL=http://ubik-api:8080/api/v1 (Docker network hostname)
#   NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1 (browser access)
