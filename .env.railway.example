# Railway Environment Variables Configuration
# Copy these to your Railway project settings

# ============================================
# API Service Environment Variables
# ============================================

# Database (provided by Railway PostgreSQL service)
DATABASE_URL=${POSTGRES_URL}

# JWT Authentication (generate with: openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server Configuration
PORT=8080
GIN_MODE=release

# Logging
LOG_LEVEL=info
LOG_FORMAT=json

# ============================================
# Next.js Web UI Environment Variables
# ============================================

# API URL (will be your Railway API service URL)
NEXT_PUBLIC_API_URL=https://ubik-api-production.up.railway.app

# NextAuth (if using authentication in Next.js)
NEXTAUTH_URL=https://ubik-web-production.up.railway.app
NEXTAUTH_SECRET=your-nextauth-secret-change-this

# Node Environment
NODE_ENV=production

# ============================================
# PostgreSQL Database (Railway Plugin)
# ============================================
# These are auto-populated by Railway when you add PostgreSQL plugin:
# - POSTGRES_URL
# - POSTGRES_HOST
# - POSTGRES_PORT
# - POSTGRES_USER
# - POSTGRES_PASSWORD
# - POSTGRES_DATABASE

# ============================================
# Optional: Application Configuration
# ============================================

# Session Configuration
SESSION_DURATION=24h
SESSION_CLEANUP_INTERVAL=1h

# Rate Limiting
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=1m

# CORS Origins (comma-separated)
CORS_ORIGINS=https://ubik-web-production.up.railway.app,https://yourdomain.com
