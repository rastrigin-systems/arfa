#!/bin/bash
#
# Pre-commit hook for Ubik Enterprise
# Automatically regenerates ERD documentation when schema changes
#
# Source models:
#   - schema.sql (database schema)
#   - openapi/spec.yaml (API specification)
#   - sqlc/queries/*.sql (SQL queries)
#   - scripts/generate-erd-overview.py (ERD generator)
#
# Generated outputs (committed to git):
#   - docs/ERD.md (user-friendly ERD)
#   - docs/README.md (technical reference)
#   - docs/schema.json (machine-readable schema)
#   - docs/schema.svg (visual diagram)
#   - docs/public.*.md (per-table docs)
#   - go.mod, go.sum (updated with generated packages)
#
# Note: generated/ (Go code) is NOT committed but is regenerated automatically
#

set -e

# Colors for output
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

echo -e "${BLUE}🔍 Checking for source model changes...${NC}"

# Get list of staged files
STAGED_FILES=$(git diff --cached --name-only)

# Check if any source models changed
SOURCE_MODELS_CHANGED=false

if echo "$STAGED_FILES" | grep -qE "^schema\.sql$|^openapi/spec\.yaml$|^sqlc/queries/.*\.sql$|^scripts/generate-erd-overview\.py$"; then
    SOURCE_MODELS_CHANGED=true
fi

if [ "$SOURCE_MODELS_CHANGED" = false ]; then
    echo -e "${GREEN}✓ No source model changes detected, skipping code generation${NC}"
    exit 0
fi

echo -e "${YELLOW}📝 Source models changed, regenerating code...${NC}"

# Check if required tools are installed
for tool in tbls oapi-codegen sqlc mockgen; do
    if ! command -v "$tool" &> /dev/null; then
        echo -e "${RED}❌ Missing required tool: $tool${NC}"
        echo -e "${YELLOW}Run: make install-tools${NC}"
        exit 1
    fi
done

# Check if Python is available (for ERD overview generation)
if ! command -v python3 &> /dev/null; then
    echo -e "${RED}❌ Missing required tool: python3${NC}"
    exit 1
fi

# Check if database is running (for ERD generation)
if ! docker ps | grep -q ubik-postgres; then
    echo -e "${YELLOW}⚠️  PostgreSQL not running. Starting database...${NC}"
    make db-up
    sleep 3
fi

# Run full code generation (ERD + API + DB + Mocks)
echo -e "${BLUE}🔧 Running: make generate${NC}"
if ! make generate; then
    echo -e "${RED}❌ Code generation failed${NC}"
    echo -e "${YELLOW}Fix the errors and try again${NC}"
    exit 1
fi

# Sync workspace instead of go mod tidy (for monorepo)
echo -e "${BLUE}🔧 Running: go work sync${NC}"
if ! go work sync; then
    echo -e "${RED}❌ go work sync failed${NC}"
    echo -e "${YELLOW}Fix the errors and try again${NC}"
    exit 1
fi

# Check what changed
DOCS_CHANGES=$(git diff --name-only docs/ 2>/dev/null || echo "")
GOMOD_CHANGES=$(git diff --name-only go.work.sum services/*/go.sum 2>/dev/null || echo "")

if [ -z "$DOCS_CHANGES" ] && [ -z "$GOMOD_CHANGES" ]; then
    echo -e "${GREEN}✓ No changes to commit${NC}"
    exit 0
fi

# Add changed files to staging
if [ -n "$DOCS_CHANGES" ]; then
    echo -e "${BLUE}📦 Adding documentation to commit...${NC}"
    git add docs/
    echo "$DOCS_CHANGES" | sed 's/^/  - /'
fi

if [ -n "$GOMOD_CHANGES" ]; then
    echo -e "${BLUE}📦 Adding workspace files to commit...${NC}"
    git add go.work.sum services/*/go.sum 2>/dev/null || true
    echo "$GOMOD_CHANGES" | sed 's/^/  - /'
fi

echo -e "${GREEN}✓ Code generation complete!${NC}"

exit 0
